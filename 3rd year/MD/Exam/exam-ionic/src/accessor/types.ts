export interface ReceivedItemModel {
    code: number;
    name: string;
}

export interface MenuItemModel {
    code: number;
    name: string;
    quantity: number;
}

export interface UpdateItemModel {
    code: number;
    quantity: number;
}